/**
* @description 
* <%= filename %> Dependencies. For custom services, filters, directives, providers, mixins, etc. that do not exists in the global space and are only depended on by this module.
* 
**/
angular.module('<%= filename %>.dependencies', [

]);